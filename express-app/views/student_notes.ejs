<% include ./partials/student_header %>

	<body>
	
		<% include ./partials/student_nav %>

			<div class="col-sm-8">
					br><br>
					<div class="container">
							<% if (note) { %>
		
							
								<% note.forEach(function(notes) { %>
									<br>
					<div class="container">
						
						<div class="card" style="width: 300px">
								
								<div class="card-body">
									<h4 class="card-title"><%= notes.title %></h4><br>
									<a href="/student/groups/view/<%= currentGroup.id %>/notes/<%= notes.link %>" class="btn btn-primary"> Download</a>				  
											
									
								</div>
								</div>
					</div>
									
								<% }); %>
								<% } %>
					</div>
					<br>
			</div>
			
		</div>
	</body>
</html>