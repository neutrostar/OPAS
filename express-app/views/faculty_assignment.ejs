<% include ./partials/faculty_header %>
	
		<% include ./partials/faculty_nav %>
			
			<div class="col-sm-8">
					<br>	
					<% if (assignments.length != 0) { %>

						<h2>Ongoing Assignments</h2>
						<br>
						<div class = "card-columns">
							<% assignments.forEach(function(assignment) { %>
								<div class="card">
									<div class="card-body">
										<% if (assignment.subject) { %>
											<span class="card-header"><%= assignment.subject.subject_id %></span>
										<% } %>	
										<p>
											<b><%= assignment.title %></b><br>
											Start date : 5/05/2018<br>
											Deadline : 10/05/2018
										</p>
									
										<div class="card-action white">
											<a href="/faculty/groups/view/<%= currentGroup._id %>/assignments/view/<%= assignment._id %>"><button type="button" class="btn btn-light">View Assignment</button></a>
											
										</div>
									</div>
								</div>
								<br>
								<% }); %>
							</div>
					<% } else { %>
						<div class="container">
							<h3><font color = "teal">Currently no assignments</font></h3>
						</div>
					<% } %>
		
				
				<br>
				<div class="container">
					<form action="/faculty/groups/view/<%= currentGroup._id %>/assignments/create">
						<button type="submit" class="btn btn-primary">Create new assignment</button>
					</form>
				</div>
					<!-- Grey navigation panel -->
				
				</div>
			</div>
		</div>
	</body>
</html>